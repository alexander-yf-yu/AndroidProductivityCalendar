<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".UserMenuActivity">

    <TextView android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:id="@+id/welcome_view"
              android:layout_margin="16dp"
              android:paddingBottom="40dp"
              android:textStyle="bold"
              android:textSize="40dp"/>

    <Button android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/calendars_button"
            android:text="@string/calendars"
            android:layout_below="@+id/welcome_view"
            android:layout_marginBottom="30dp" android:onClick="openMainMenu"/>

    <Button android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/messages_button"
            android:text="@string/messages"
            android:layout_below="@+id/calendars_button"
            android:layout_marginBottom="30dp"
            android:onClick="showMessages"/>

    <Button android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/alerts_button"
            android:layout_below="@+id/messages_button"
            android:onClick="showAlerts"/>





</RelativeLayout>